services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: engnova
      POSTGRES_PASSWORD: engnova
      POSTGRES_DB: engnova
    volumes:
      - engnova_pg:/var/lib/postgresql/data

volumes:
  engnova_pg:
